# Contraindication model

`Contraindication` is an active terminology model with a unique `name`, an optional `description`, and natural-key lookup. Base data is loaded by `load_contraindication_data`, which is included in `load_base_db_data`.
