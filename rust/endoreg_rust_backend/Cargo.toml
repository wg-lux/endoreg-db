[package]
name = "endoreg_rust_backend"
version = "0.1.0"
edition = "2021"

[lib]
name = "endoreg_rust_backend"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
pyo3-stub-gen = { version = "0.6", default-features = false }
sha2 = "0.10"
